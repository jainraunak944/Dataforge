# Build status

Last update: 2026-09-05 (session start)

## Completed
- Read full 14-page brief (`Pathway PS.pdf`); extracted text + all hyperlinks.
- `docs/CLAIM.md` written before application code.
- `docs/REQUIREMENTS.md` requirement matrix (all statuses honestly `pending`).
- Continuity files created.

## In flight
- 3 background research agents fetching/verifying: BDH paper + Pathway pages + official repo; BDH-CQ report (post-cutoff, must be fetched); DeltaNet/Gated DeltaNet/Schlag2021. They write `docs/research-notes/{bdh,bdh-cq,delta-rule}.md`.

## Decisions
- Stack: React 18 + TypeScript + Vite 5, KaTeX for equations, hand-rolled SVG visuals, Vitest for math tests, Playwright (preinstalled Chromium at /opt/pw-browsers/chromium) for smoke.
- App lives at repo root (`src/`), docs in `docs/`, submission in `submission/`.
- PDFs generated by headless Chromium print-to-PDF from HTML sources (reproducible via `npm run pdfs`).
- Deployment: GitHub Pages workflow prepared; enabling Pages = external gate.
- License: MIT for original code (to confirm in PROVENANCE).

## Blockers
- None yet. External gates expected at deployment step.

## Test results
- None run yet.

## Next command
Scaffold Vite app + implement `src/memory/` core and tests.
